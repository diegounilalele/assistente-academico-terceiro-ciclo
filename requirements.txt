# Dependências Python do projeto (instale com: pip install -r requirements.txt)
# Só os pacotes de terceiros — sqlite3, json, os, re, time etc. já vêm com o Python,
# e o werkzeug (hash de senha) é instalado junto com o Flask.
Flask==3.1.3
python-dotenv==1.2.2
pyngrok==8.1.2
requests==2.34.2
